# ============================================
# Radio Calico - Docker Environment Configuration
# ============================================
# Copy this file to .env and customize as needed

# Stream URL - MP3 320kbps live radio stream
# This is the primary configuration for the radio stream
STREAM_URL=https://radio3.radio-calico.com:8443/calico.mp3

# Server Port
# The port the application will listen on inside the container
# External port mapping is configured in docker-compose.yml
PORT=3000

# Node Environment
# Options: development, production
# Set by docker-compose automatically, but can be overridden
# NODE_ENV=production

# ============================================
# Optional Configuration
# ============================================

# Uncomment to use a different stream URL
# STREAM_URL=https://your-stream-server.com/stream.mp3

# Uncomment to change the internal port (also update docker-compose.yml)
# PORT=8080
